export let hsla: [number, string, string, number] = [10,'high','high',0.1];
/**HSLA = hew  saturation luminance  alpha */

/**named tuple is used when we don't know the seq of variables and types so we name the var and its type */
export let course:[name: string, id: number];
